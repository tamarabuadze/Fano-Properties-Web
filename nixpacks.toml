[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g pnpm@11.3.0 && pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter @fano/db db:generate && pnpm --filter @fano/web build"
]

[start]
cmd = "pnpm --filter @fano/web start"
